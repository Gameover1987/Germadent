<UserControl x:Class="Germadent.Rma.App.Views.PriceListEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Germadent.Rma.App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="0.65*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Ценовые группы"
                   Style="{StaticResource GroupNameStyle}"/>

        <ListBox Grid.Row="1" Grid.Column="0" Margin="0,6,0,0"></ListBox>

        <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal" Margin="0,6,0,0">
            <Button Content="Добавить" Width="100"></Button>
            <Button Content="Редактировать" Width="100" Margin="6,0,0,0"></Button>
            <Button Content="Удалить" Width="100" Margin="6,0,0,0"></Button>
        </StackPanel>

        <TextBlock Grid.Row="0" Grid.Column="1" Text="Ценовые позиции" Margin="12,0,0,0"
                   Style="{StaticResource GroupNameStyle}"/>

        <ListBox Grid.Row="1" Grid.Column="1" Margin="12,6,0,0"></ListBox>

        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="12,6,0,0">
            <Button Content="Добавить" Width="100"></Button>
            <Button Content="Редактировать" Width="100" Margin="6,0,0,0"></Button>
            <Button Content="Удалить" Width="100" Margin="6,0,0,0"></Button>
        </StackPanel>

    </Grid>
</UserControl>
